{
  "variables": {
    "name": "Ubuntu 26.04 LTS",
    "source_ami": "ami-01d8997217a233789",
    "access_key": "",
    "secret_key": "",
    "region": "us-east-2"
  },
  "builders": [
    {
      "type": "amazon-ebs",
      "access_key": "{{user `access_key`}}",
      "secret_key": "{{user `secret_key`}}",
      "ami_name": "{{user `name`}}",
      "ami_description": "Ubuntu 26.04 base image",
      "region": "{{user `region`}}",
      "ami_regions": [
        "us-east-1",
        "us-east-2",
        "us-west-1",
        "us-west-2"
      ],
      "source_ami": "{{user `source_ami`}}",
      "instance_type": "t3.medium",
      "communicator": "ssh",
      "ssh_username": "ubuntu",
      "force_deregister": true,
      "run_tags": {
        "Name": "{{user `name`}}"
      }
    }
  ],
  "provisioners": []
}